NEXT_PUBLIC_STRAPI_URL=http://localhost:1338
# Set on non-production deployments only (staging, preview). Shows a banner so
# nobody mistakes one environment for the other — the UIs are identical and the
# actions are not read-only.
# NEXT_PUBLIC_PULSE_ENV=staging
